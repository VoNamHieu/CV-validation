node_modules/
.next/
venv/
__pycache__/
*.pyc
.env
.env.local
.env*.local
.DS_Store
.vercel

# Local SQLite caches (company_cache.db, etc.)
backend/data/
*.db
*.db-journal
*.db-wal
*.db-shm

# packaged extension build artifact (rebuild: zip -r extension.zip extension)
extension.zip

# Internal-only DOM research extension — lives on the `internal/research-extension`
# branch (force-added there). Ignored everywhere else so it never gets swept into
# a product commit or the user-facing extension build.
tools/research-extension/
